services:
  django:
    env_file:
      - .env

    environment:
      DJANGO_SETTINGS_MODULE: config.settings.production

    command:
      - gunicorn
      - config.wsgi:application
      - --bind
      - 0.0.0.0:8000
      - --workers
      - "2"

  postgres:
    env_file:
      - .env