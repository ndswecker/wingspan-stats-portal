services:
  django:
    env_file:
      - .env

    environment:
      DJANGO_SETTINGS_MODULE: config.settings.development

    volumes:
      - .:/workspace

    command: /workspace/docker/entrypoint.sh

  postgres:
    env_file:
      - .env

    ports:
      - "5432:5432"